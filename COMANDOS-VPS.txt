═══════════════════════════════════════════════════════════════
🚀 COMANDOS RÁPIDOS - ARK DEPLOY VPS
═══════════════════════════════════════════════════════════════

📥 ATUALIZAR CÓDIGO E APLICAR CORREÇÕES
───────────────────────────────────────────────────────────────
cd /opt/ark-deploy
git pull origin main
./scripts/configure-vps.sh


🔧 CONFIGURAR MANUALMENTE (se preferir)
───────────────────────────────────────────────────────────────
cd /opt/ark-deploy
nano .env

# Adicionar esta linha:
NEXT_PUBLIC_API_URL=http://api.38.242.213.195.sslip.io/api

# Salvar: Ctrl+O, Enter, Ctrl+X


🔨 REBUILD DO FRONTEND (necessário após mudar NEXT_PUBLIC_API_URL)
───────────────────────────────────────────────────────────────
docker-compose down
docker-compose build --no-cache frontend
docker-compose up -d


🔍 VERIFICAR SE FUNCIONOU
───────────────────────────────────────────────────────────────
# Ver se API URL está correta:
docker-compose logs frontend | grep "API URL"

# Ver status dos containers:
docker-compose ps

# Ver logs em tempo real:
docker-compose logs -f backend frontend


🧪 TESTAR LOGIN
───────────────────────────────────────────────────────────────
curl -X POST http://api.38.242.213.195.sslip.io/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"superadmin@arkdeploy.com","password":"Admin123"}'


🔐 CREDENCIAIS
───────────────────────────────────────────────────────────────
Email: superadmin@arkdeploy.com
Senha: Admin123


🌐 URLs
───────────────────────────────────────────────────────────────
Frontend: http://painel.38.242.213.195.sslip.io
API: http://api.38.242.213.195.sslip.io/api
Health: http://api.38.242.213.195.sslip.io/api/health


📊 DIAGNÓSTICO
───────────────────────────────────────────────────────────────
# Ver configurações do MongoDB:
docker-compose exec mongodb mongosh -u admin -p 'vQO20N8X8k41oRkAUWAEnw==' --authenticationDatabase admin ark-deploy --eval "db.systemsettings.find().pretty()"

# Ver usuários:
docker-compose exec mongodb mongosh -u admin -p 'vQO20N8X8k41oRkAUWAEnw==' --authenticationDatabase admin ark-deploy --eval "db.users.find({}, {email: 1, name: 1, role: 1}).pretty()"


🔄 REINICIAR TUDO
───────────────────────────────────────────────────────────────
docker-compose restart


🛑 PARAR E INICIAR
───────────────────────────────────────────────────────────────
docker-compose down
docker-compose up -d


🧹 REBUILD COMPLETO (se tiver problemas)
───────────────────────────────────────────────────────────────
docker-compose down
docker rmi ark-deploy-frontend ark-deploy-backend
docker-compose build --no-cache
docker-compose up -d


═══════════════════════════════════════════════════════════════
